services:
  api:
    build:
      context: ./src/api
    container_name: api
    environment:
      - FOO=bar
  web:
    build:
      context: ./src/web
    container_name: web
    ports:
      - "8000:8080"
    depends_on:
      - api
